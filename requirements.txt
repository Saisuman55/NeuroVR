# NeuroVR 3D — Python Dependencies
# Research prototype · Not for clinical use
#
# Install: pip install -r requirements.txt

# ── Core ML ───────────────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0          # Required by some MONAI transforms

# ── MONAI (Medical Open Network for AI) ───────────────────────────────────────
monai>=1.3.0                  # 3D medical AI framework + BraTS model zoo

# ── 3D MRI I/O ────────────────────────────────────────────────────────────────
nibabel>=5.0.0                # NIfTI (.nii/.nii.gz) read/write
pydicom>=2.4.0                # DICOM series loading

# ── 3D Image Processing ───────────────────────────────────────────────────────
scipy>=1.10.0                 # Resampling, morphological operations
scikit-image>=0.20.0          # Marching Cubes, connected components, resize

# ── 3D Mesh Generation / GLB Export ──────────────────────────────────────────
trimesh>=4.0.0                # Mesh smoothing, export

# ── Numerics ──────────────────────────────────────────────────────────────────
numpy>=1.24.0

# ── Web Server ────────────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0

# ── Configuration ─────────────────────────────────────────────────────────────
PyYAML>=6.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0                  # Progress bars during model download

# ── Image I/O (2D slice PNG export for split-view) ───────────────────────────
Pillow>=10.0.0
h5py>=3.8.0                  # Cheng CE-MRI MATLAB/HDF5 training dataset

# ── Visualization (2D slice rendering fallback) ───────────────────────────────
matplotlib>=3.7.0

# ── MONAI model downloader ────────────────────────────────────────────────────
huggingface_hub>=0.19.3,<0.25.0   # Used by scripts/download_bundle.py

# ── Training pipeline additions ───────────────────────────────────────────────
pandas>=2.0.0                 # Training history CSV export
matplotlib>=3.7.0             # Training curve plots
tensorboard>=2.14.0           # Optional TensorBoard logging
pyyaml>=6.0                   # Config file parsing
